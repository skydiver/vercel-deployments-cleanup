{
  "name": "vercel-deployments-cleanup",
  "version": "1.0.0",
  "description": "Automatically remove old preview deployments",
  "license": "MIT",
  "author": "Martín M.",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@actions/core": "^1.8.2",
    "@actions/github": "^5.0.3",
    "@vercel/ncc": "^0.34.0",
    "node-fetch": "^2.6.7"
  }
}
